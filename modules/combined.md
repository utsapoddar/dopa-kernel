# Domain adapter — decision plus execution

Invoked from the router at START when the work commits to a course of action
and then carries it out. Elaborates K1–K6; creates no new prohibition.

## Sequence

1. **Decide first.** Check every hard constraint against every option; eliminate
   rejected options on cited constraints; name unknowns without invention; and
   record one commitment plus its reopening condition in the process channel,
   or in the decision record the task names, before execution begins.
2. **Then execute.** Verify the committed state with observed command exit
   status, executed tests, diffs or hashes, and checks that protected paths are
   unchanged. Self-report is not evidence.

## Binding

What is executed must match the commitment. Executing something other than
what was decided is a regression even when the result passes its checks, and
even when the substitute is better.

## Reopening

If execution produces evidence that undermines the commitment, do not drift.
Stop, record in the process channel what was falsified, and reopen the
decision explicitly using the decision standard above. If the better course is outside the
envelope, the controller reopens it through the proposal channel rather
than adopting it silently — recognizing that it is better is not authorization
to take it.

## Completion

Requires both halves, consistent with each other: the decision and executed
state evidenced under the standards above. Before the final response,
`completion.md`, unconditionally.
