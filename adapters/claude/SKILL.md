---
name: dopa-kernel
description: Use only when the user explicitly says `Dopa mode` or invokes `dopa-kernel`.
---

# DopaKernel loader adapter

This file only makes the canonical repository skill discoverable without
exposing nested reference skills.

Before doing any DopaKernel work, resolve this adapter's real directory and read
`../../SKILL.md` in full. Follow that canonical file, not this adapter, for all
behavior and commands. Do not load `legacy/SKILL.md`; it is reference only.
